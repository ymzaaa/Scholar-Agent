# 管理员与维护命令

本目录只保存低频管理员命令，不包含产品运行主流程。正式 Word → LaTeX CLI 仍为 `stage1/run_pipeline.py`。

从仓库根目录运行：

~~~powershell
python -m stage1.commands.run_template_baselines --help
~~~

新模板注册、规范 Word 转换和模板审查命令不属于当前公开运行主线，也不会由正式运行路径加载。
