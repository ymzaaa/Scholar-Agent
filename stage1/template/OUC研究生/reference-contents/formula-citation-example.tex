% 仅供模板使用者参考，不由 main.tex 默认引用。

行内公式示例为 $a+b=c$，行间公式示例如下：
\begin{equation}
  E = mc^2
  \label{eq:example}
\end{equation}

正文数字引用应由转换流程绑定到参考文献条目，再渲染为模板支持的
% \cite{confirmed-reference-key}
命令；示例不提供或伪造真实参考文献元数据。

